machine:
  node:
    version: 4.4.7

dependencies:
  override:
    - npm install --dev

test:
  post:
    # Ensure steps leading up to publishing work.
    - node_modules/.bin/builder run transpile-dev