pkg_help_description: >
    Dash ships with supercharged components for
    interactive user interfaces. A core set of components,
    written and maintained by the Dash team, is available in
    the dashCoreComponents package. The source for this package
    is on GitHub: plotly/dash-core-components.
pkg_help_title: >
    Core Interactive UI Components for Dash 

r_examples:
    - name: dccChecklist
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccChecklist(
                    options=list(
                      list("label" = "New York City", "value" = "NYC"),
                      list("label" = "Montréal", "value" = "MTL"),
                      list("label" = "San Francisco", "value" = "SF")
                    ),
                    value=list("MTL", "SF")
                  )
                )
              )
            )

            app$run_server()
    - name: dccConfirmDialog
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccConfirmDialog(
                    id = "confirm",
                    message = "Danger danger! Are you sure you want to continue?",
                    displayed = TRUE
                  )
                )
              )
            )

            app$run_server()
    - name: dccConfirmDialogProvider
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccConfirmDialogProvider(
                    id = "confirm",
                    children = htmlButton("Click Me"),
                    message = "Danger danger! Are you sure you want to continue?"
                  )
                )
              )
            )

            app$run_server()
    - name: dccDatePickerRange
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccDatePickerRange(
                    id = "date-picker-range",
                    start_date = as.Date("1997/5/10"),
                    end_date_placeholder_text="Select a date!"
                  )
                )
              )
            )

            app$run_server()
    - name: dccDatePickerSingle
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccDatePickerSingle(
                      id="date-picker-single",
                      date=as.Date("1997/5/10")
                  )
                )
              )
            )

            app$run_server()
    - name: dccDropdown
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccDropdown(
                    options=list(
                      list(label = "New York City", value = "NYC"),
                      list(label = "Montréal", value = "MTL"),
                      list(label = "San Francisco", value = "SF")
                    ),
                    value="MTL"
                  )
                )
              )
            )

            app$run_server()
    - name: dccGraph
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccGraph(
                    id = "my-graph",
                    figure = list(
                      data = list(
                        list(x = c(1, 2, 3), y = c(5, 2, 7))
                      )
                    )
                  )
                )
              )
            )

            app$run_server()
    - name: dccInput
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccInput(
                    placeholder = "Enter a value...",
                    type = "text",
                    value = ""
                  )
                )
              )
            )

            app$run_server()
    - name: dccInterval
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  htmlH1(id = "my-h1", children = "0"),
                  dccInterval(
                    id = "my-interval",
                    interval=1*1000, # in milliseconds 
                    n_intervals = 0
                  )
                )
              )
            )

            app$callback(
              output("my-h1", "children"),
              list(input("my-interval", "n_intervals")),
              function(x){
                x
              }
            )

            app$run_server()
    - name: dccLink
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccLocation(id = "url", refresh = FALSE),
                  dccLink("navigate to page 1", href = "/page1"),
                  dccLink("navigate to page 2", href = "/page2"),
                  htmlDiv(id = "page-content")
                )
              )
            )

            app$callback(
              output("page-content", "children"),
              list(input("url", "pathname")),
              function(x){
                htmlH1(sprintf("You are on %s", x)) 
              }
            )

            app$run_server()
    - name: dccLoading
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccLoading(
                    children = dccGraph(
                      figure = list(
                        data = list(
                            list(x = c(1,2,3), y = c(2,5,7))
                        )
                      )
                    )
                  )
                )
              )
            )

            app$run_server()
    - name: dccLocation
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccLocation(id = "url", refresh = FALSE),
                  dccLink("navigate to page 1", href = "/page1"),
                  dccLink("navigate to page 2", href = "/page2"),
                  htmlDiv(id = "page-content")
                )
              )
            )

            app$callback(
              output("page-content", "children"),
              list(input("url", "pathname")),
              function(x){
                htmlH1(sprintf("You are on %s", x)) 
              }
            )

            app$run_server()
    - name: dccLogoutButton
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccLogoutButton(
                    label = "logout", logout_url = "https://dashr.plot.ly/"
                  )
                )
              )
            )

            app$run_server()
    - name: dccMarkdown
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccMarkdown("
                    Dash supports [Markdown](http://commonmark.org/help).
                    Markdown is a simple way to write and format text.
                    It includes a syntax for things like **bold text** and *italics*,
                    [links](http://commonmark.org/help), inline `code` snippets, lists,
                    quotes, and more."
                  )
                )
              )
            )

            app$run_server()
    - name: dccRadioItems
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccRadioItems(
                    options=list(
                      list("label" = "New York City", "value" = "NYC"),
                      list("label" = "Montréal", "value" = "MTL"),
                      list("label" = "San Francisco", "value" = "SF")
                    ),
                    value = "MTL"
                  )
                )
              )
            )

            app$run_server()
    - name: dccRangeSlider
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccRangeSlider(
                    count = 1,
                    min = -5,
                    max = 10,
                    step = 0.5,
                    value = list(-3, 7)
                  )
                )
              )
            )

            app$run_server()
    - name: dccSlider
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccSlider(
                    min = 0,
                    max = 9,
                    marks = 0:9, 
                    value = 5,
                  )
                )
              )
            )

            app$run_server()
    - name: dccStore
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccStore(
                    id = "my-store", 
                    data = list("my-data" = "data")
                  )
                )
              )
            )

            app$run_server()
    - name: dccTab
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccTabs(
                    children = list(
                      dccTab(
                        label = "tab1",
                        children = htmlH1("Tab 1 Contents")
                      ),
                      dccTab(
                        label = "tab2",
                        children = htmlH1("Tab 2 Contents")
                      )
                    )
                  )
                )
              )
            )

            app$run_server()
    - name: dccTabs
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccTabs(
                    children = list(
                      dccTab(
                        label = "tab1",
                        children = htmlH1("Tab 1 Contents")
                      ),
                      dccTab(
                        label = "tab2",
                        children = htmlH1("Tab 2 Contents")
                      )
                    )
                  )
                )
              )
            )

            app$run_server()
    - name: dccTextarea
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccTextarea(
                    placeholder = "Enter a value...",
                    value = "This is a TextArea component",
                    style = list("width" = "100%")
                  )     
                )
              )
            )

            app$run_server()
    - name: dccUpload
      dontrun: TRUE
      code: |
            library(dash)
            library(dashHtmlComponents)
            library(dashCoreComponents)

            app <- Dash$new()

            app$layout(
              htmlDiv(
                list(
                  dccUpload(
                    id = "upload-data",
                    children = htmlH1("select files"),
                    style = list(border = "1px dashed")
                  )
                )
              )
            )

            app$run_server()
